# Copie este conteúdo para o arquivo requirements.txt na raiz do repositório rombo-app.
# Objetivo: deploy rápido no Streamlit Community Cloud (sem TensorFlow / ML / etc.).
#
# IMPORTANTE: no painel do app (Settings → Python version), escolha 3.11 ou 3.12.
# Evite 3.14 — muitas libs ainda não têm wheel e o pip compila tudo (demora e pode falhar).

streamlit>=1.45,<2
pandas>=2.0,<2.3
numpy>=1.24,<2.4
pyarrow>=14,<20
toml>=0.10,<1
plotly>=5,<7
duckdb>=1.0,<2
Pillow>=10,<13
